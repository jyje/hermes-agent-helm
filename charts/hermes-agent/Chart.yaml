apiVersion: v2
name: hermes-agent
description: |
  👩🏻‍💻 Hermes Agent on Kubernetes - sign in with Codex/Copilot, run agent teams, stay lightweight.
  
  Run [Hermes Agent](https://github.com/NousResearch/hermes-agent) - a multi-provider LLM agent framework - on Kubernetes. Configure any provider Hermes supports (OpenAI, Anthropic, Gemini, OpenRouter, NVIDIA, or any OpenAI-compatible proxy such as LiteLLM/vLLM) entirely via values.yaml, with a built-in helm test health check.
type: application

icon: https://hermes-agent.nousresearch.com/docs/img/logo.png

# Chart version (bump on chart changes).
version: 1.13.0

# Upstream Hermes Agent image version this chart targets by default.
# Date-based tag; corresponds to Hermes Agent v0.16.0 (2026.6.5).
appVersion: "v2026.8.31"

keywords:
  - hermes
  - agent
  - llm
  - openai-compatible

home: https://github.com/jyje/hermes-agent-helm
sources:
  - https://github.com/jyje/hermes-agent-helm
  - https://github.com/NousResearch/hermes-agent
maintainers:
  - name: jyje
    email: jyjeon@outlook.com

# Artifact Hub metadata (for future publishing to artifacthub.io).
annotations:
  artifacthub.io/category: ai-machine-learning
  artifacthub.io/license: MIT
  artifacthub.io/changes: |
    - kind: "fixed"
      description: "Documentation(approvals): Cover unattended_mode and protected instruction files Add `approvals.unattended_mode` (new upstream between v2026.8.27 and v2026.8...."
    - kind: "fixed"
      description: "Documentation(dashboard): Describe the real dashboard lifecycle and make values-ingress.yaml work The chart described the management dashboard as \"binds 127...."
    - kind: "added"
      description: "Fix(config): Stop seeding agent.max_turns, following upstream's unlimited default The chart has seeded `config.agent.max_turns: 90` since its first commit. T..."
    - kind: "fixed"
      description: "Documentation(env): Refresh the curated environment-variable table for v2026.8.31 Re-check the chart README's \"Environment variables\" teaser table against th..."
    - kind: "added"
      description: "Feature(secrets): Render an ExternalSecret for External Secrets Operator users Add `externalSecret.*` values. Setting `externalSecret.enabled: true` renders..."
    - kind: "added"
      description: "Update the default Hermes Agent image to v2026.8.31."
