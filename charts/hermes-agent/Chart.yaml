apiVersion: v2
name: hermes-agent
description: |
  👩🏻‍💻 Hermes Agent on Kubernetes - sign in with Codex/Copilot, run agent teams, stay lightweight.
  
  Run [Hermes Agent](https://github.com/NousResearch/hermes-agent) - a multi-provider LLM agent framework - on Kubernetes. Configure any provider Hermes supports (OpenAI, Anthropic, Gemini, OpenRouter, NVIDIA, or any OpenAI-compatible proxy such as LiteLLM/vLLM) entirely via values.yaml, with a built-in helm test health check.
type: application

icon: https://hermes-agent.nousresearch.com/docs/img/logo.png

# Chart version (bump on chart changes).
version: 1.11.0

# Upstream Hermes Agent image version this chart targets by default.
# Date-based tag; corresponds to Hermes Agent v0.16.0 (2026.6.5).
appVersion: "v2026.8.19"

keywords:
  - hermes
  - agent
  - llm
  - openai-compatible

home: https://github.com/jyje/hermes-agent-helm
sources:
  - https://github.com/jyje/hermes-agent-helm
  - https://github.com/NousResearch/hermes-agent
maintainers:
  - name: jyje
    email: jyjeon@outlook.com

# Artifact Hub metadata (for future publishing to artifacthub.io).
annotations:
  artifacthub.io/category: ai-machine-learning
  artifacthub.io/license: MIT
  artifacthub.io/changes: |
    - kind: "fixed"
      description: "Documentation(chart): Align the chart tagline with the repo description Sync `Chart.yaml`'s description, the chart README (English and Korean), and the docs-..."
    - kind: "added"
      description: "Update the default Hermes Agent image to v2026.8.19."
    - kind: "fixed"
      description: "Documentation(docs): Add a Production Checklist to the repo README Add a \"Production Checklist\" table (English and Korean) covering Pod Security Standards, e..."
    - kind: "added"
      description: "Feature(values): Ship a CI-verified Pod Security Standards hardening profile Add `auth.deviceFlow.securityContext` and `team.sharedVolume.permissions.securit..."
