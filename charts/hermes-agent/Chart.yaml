apiVersion: v2
name: hermes-agent
description: |
  👩🏻‍💻 Hermes Agent on Kubernetes - sign in with Codex/Copilot, run agent teams, stay lightweight.
  
  Run [Hermes Agent](https://github.com/NousResearch/hermes-agent) - a multi-provider LLM agent framework - on Kubernetes. Configure any provider Hermes supports (OpenAI, Anthropic, Gemini, OpenRouter, NVIDIA, or any OpenAI-compatible proxy such as LiteLLM/vLLM) entirely via values.yaml, with a built-in helm test health check.
type: application

icon: https://hermes-agent.nousresearch.com/docs/img/logo.png

# Chart version (bump on chart changes).
version: 1.16.0

# Upstream Hermes Agent image version this chart targets by default.
# Date-based tag; corresponds to Hermes Agent v0.16.0 (2026.6.5).
appVersion: "v2026.9.24"

keywords:
  - hermes
  - agent
  - llm
  - openai-compatible

home: https://github.com/jyje/hermes-agent-helm
sources:
  - https://github.com/jyje/hermes-agent-helm
  - https://github.com/NousResearch/hermes-agent
maintainers:
  - name: jyje
    email: jyjeon@outlook.com

# Artifact Hub metadata (for future publishing to artifacthub.io).
annotations:
  artifacthub.io/category: ai-machine-learning
  artifacthub.io/license: MIT
  artifacthub.io/changes: |
    - kind: "added"
      description: "Fix(persistence): Migrate persisted Hermes config Run Hermes' non-interactive config migration after chart seeding, with backups and a documented recovery pa..."
    - kind: "added"
      description: "Feature(examples): Add a Google Chat Pub/Sub example Add `values-google-chat.yaml`, which runs Hermes as a Google Chat bot over a Cloud Pub/Sub pull subscrip..."
    - kind: "added"
      description: "Update the default Hermes Agent image to v2026.9.14."
    - kind: "added"
      description: "Update the default Hermes Agent image to v2026.9.21."
    - kind: "fixed"
      description: "Documentation(providers): Sign in to OpenRouter from a running pod Document `hermes auth add openrouter --type oauth` (new in `v2026.9.14`) as an explicit, i..."
    - kind: "fixed"
      description: "Documentation(readme): Nav row line break Separate the language-switcher row from the repository nav row (Hermes Team, Chart docs, CONTRIBUTING, SECURITY, AG..."
    - kind: "fixed"
      description: "Documentation(storage): Explain which filesystems suit HERMES_HOME's SQLite databases Explain that a PVC access mode does not establish the file locking and..."
