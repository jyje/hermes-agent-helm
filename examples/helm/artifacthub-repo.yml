# Artifact Hub repository metadata (ownership verification).
#
# Push this to the same OCI path as the chart so Artifact Hub can verify you own
# the repository, e.g.:
#   oras push ghcr.io/jyje/charts/hermes-agent:artifacthub.io \
#     artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml
# (For a classic HTTP Helm repo, place this file next to index.yaml instead.)
#
# repositoryID is shown in Artifact Hub after you add the repo in the Control Panel.
repositoryID: REPLACE_WITH_ARTIFACT_HUB_REPO_ID
owners:
  - name: jyje
    email: jyjeon@outlook.com
