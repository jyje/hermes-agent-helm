---
"@jyje/hermes-agent-helm": patch
---

Documentation(docs): Replace em-dashes with colons or plain dashes

Convert every em-dash in the chart README, the values examples, and the
JSON schema description to a colon or a plain dash, so the shipped
documentation stays easy to type, grep, and read in a terminal. Wording
and every configuration key are unchanged.
